
const appBar = {
  borderRadius: 15,
  margin: '30px 0',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'center',
  alignItems: 'center',
}

const heading = { color: '#FF2E60' }
const image = { marginLeft: '15px' }

module.exports = { appBar, heading, image }